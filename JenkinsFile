pipeline{
	agent any
	stages{
			stage('Maven Compile'){
				def mvnHome = tool name: 'maven-3', type: 'maven'
				def mvnCmd = "${mvnHome}/bin/mvn"
				echo 'Compile Stage'
				sh "${mvnCmd} compile"
			}
			stage('Maven Test'){
				steps{
				echo 'Test Stage'
				}
			}
			
			
	}
	


}
